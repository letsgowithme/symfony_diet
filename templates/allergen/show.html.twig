{% extends 'base.html.twig' %}

{% block title %}Allergen{% endblock %}

{% block body %}
    <h1>Allergen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ allergen.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ allergen.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('allergen.index') }}">Liste d'allergènes</a>

    <a href="{{ path('allergen.edit', {'id': allergen.id}) }}">Modifier</a>

   
{% endblock %}
