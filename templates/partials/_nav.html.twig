<nav class="navbar navbar-expand-lg navbar-dark bg-primary text-light">
	<div class="container-fluid">
		<a class="navbar-brand" href="#"></a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto text-center">
				<li class="nav-item">
					<a class="nav-link active fw-bold" href="/">Accueil
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link fw-bold" href="{{ path('contact.index') }}">Contact</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item">
						<a class="nav-link fw-bol" href="{{ path('recipe.recipes') }}">
							Collection de recettes</a>
					</li>
				{% elseif app.user %}
					<li class="nav-item">
						<a class="nav-link fw-bold" href="{{ path('recipe.index') }}">Mes recettes</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link fw-bold" href="{{ path('recipe.index_public') }}">Recettes</a>
					</li>
				{% endif %}
			</ul>
			{% if not app.user %}
			<ul class="navbar-nav lg-me-1 d-lg-flex align-items-lg-end text-center">
				<li class="nav-item me-2 fw-bold">
					<a class="nav-link" href="/connexion">Connexion</a>
				</li>
				<li class="nav-item lg-me-4 fw-bold">
					<a class="nav-link" href="/inscription">Inscription</a>
				</li>
			</ul>
			{% endif %}
		</div>
	</div>
	<div class="d-flex">
		<ul class="navbar-nav me-auto">
			{% if app.user %}
				<li class="nav-item dropdown" style="margin-right: 5rem;">
					<a class="nav-link dropdown-toggle fs-5 fw-bold" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false" style="color: white;">{{ app.user.fullName }}</a>
					<div class="dropdown-menu bg-primary">
						{% if 'ROLE_ADMIN' in app.user.roles %}
							<a class="dropdown-item  fw-bold" href="/admin">Tableau de bord</a>
						{% endif %}
						<div class="dropdown-divider"></div>
						<a class="dropdown-item fw-bold" href="{{ path('security.logout') }}">Déconnextion</a>
										{% endif %}
				</li>
			</div>
		</li>
	</ul>
</div></div></nav>
